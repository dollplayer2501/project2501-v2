{
  "name": "project2501-v2",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "develop:ncu:check": "ncu",
    "develop:ncu:update": "ncu -u",
    "develop:clean": "rimraf ./_develop",
    "develop:gulp:build:stylesheet": "cross-env NODE_ENV=develop npx gulp stylesheet",
    "develop:gulp:build:javascript": "cross-env NODE_ENV=develop npx gulp javascript",
    "develop:gulp:build:imageWebp": "cross-env NODE_ENV=develop npx gulp imageWebp",
    "develop:gulp:build": "cross-env NODE_ENV=develop npx gulp build",
    "develop:gulp:watch": "cross-env NODE_ENV=develop npx gulp watch",
    "develop:eleventy:build": "cross-env NODE_ENV=develop npx @11ty/eleventy",
    "develop:eleventy:watch": "cross-env NODE_ENV=develop npx @11ty/eleventy --serve --watch --port=8080",
    "develop:build": "npm-run-all --sequential develop:clean --parallel develop:gulp:build develop:eleventy:build",
    "develop:watch": "npm-run-all --sequential develop:clean develop:gulp:build --parallel develop:gulp:watch develop:eleventy:watch",
    "product:clean": "rimraf ./_production",
    "product:gulp:build": "cross-env NODE_ENV=production npx gulp build",
    "product:eleventy:build": "cross-env NODE_ENV=production npx @11ty/eleventy",
    "product:build": "npm-run-all --sequential product:clean --parallel product:gulp:build product:eleventy:build",
    "product:serve": "npx serve ./_production -p 3000"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@11ty/eleventy": "^2.0.1",
    "@11ty/eleventy-plugin-directory-output": "^1.0.1",
    "cross-env": "^7.0.3",
    "gulp": "^4.0.2",
    "gulp-dart-sass": "^1.1.0",
    "gulp-if": "^3.0.0",
    "gulp-mode": "^1.1.0",
    "gulp-sourcemaps": "^3.0.0",
    "gulp-terser": "^2.1.0",
    "gulp-webp": "^5.0.0",
    "html-minifier": "^4.0.0",
    "npm-check-updates": "^16.14.6",
    "npm-run-all": "^4.1.5",
    "rimraf": "^5.0.5",
    "serve": "^14.2.1"
  }
}
